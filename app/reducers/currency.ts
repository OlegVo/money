
const CURRENCY = '₽';
// const CURRENCY = '';

const reducer = (state: string = CURRENCY, action): string => {
    switch (action.type) {
        default:
            return state;
    }
};

export default reducer;
